import { PageShell } from "@/components/ui/page-shell";

export default function SettingsPage() {
  return <PageShell title="Settings" description="Users, stages, products, inventory defaults, and role controls." />;
}
