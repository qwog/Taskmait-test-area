import { PageShell } from "@/components/ui/page-shell";

export default function DealsPage() {
  return <PageShell title="Deals" description="Pipeline with table + kanban workflow and weighted forecasting." />;
}
