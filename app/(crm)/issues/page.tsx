import { PageShell } from "@/components/ui/page-shell";

export default function IssuesPage() {
  return <PageShell title="Issues" description="Plan monthly Localendar issues and monitor slot occupancy + revenue." />;
}
